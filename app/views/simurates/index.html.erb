<p style="color: green"><%= notice %></p>

<h1>Simurates</h1>

<div id="simurates">
  <% @simurates.each do |simurate| %>
    <%= render simurate %>
    <p>
      <%= link_to "Show this simurate", simurate %>
    </p>
  <% end %>
</div>

<%= link_to "New simurate", new_simurate_path %>
<%= button_tag 'Start', {id: "startbutton", class: "aa"} %>

<div class="svg-wrapper">
  <div id="ob1">(゜∀゜)</div>
  <div id="ob2">(つ´∀｀)つ</div>
  <svg id="svg01" xmlns="http://www.w3.org/2000/svg" width="1000" height="500">
    <path class="root1" fill=none stroke="red" d="M 0 0 L 200 300 "/>
    <path class="root2" fill=none stroke="red" d="M 200 300 L 0 150 "/>
    <path class="root3" fill=none stroke="red" d="M 0 150 L 0 0 "/>
  </svg>
</div>

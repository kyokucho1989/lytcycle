<%= form_with(model: simurate) do |form| %>
  <% if simurate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(simurate.errors.count, "error") %> prohibited this simurate from being saved:</h2>

      <ul>
        <% simurate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
